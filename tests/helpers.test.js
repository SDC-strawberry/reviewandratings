const help = require('./helpers.js');

describe("tests out the review return function", () => {


  test(" Should return an object", () => {
    expect(help.returnReviews([], 1234)).toBe(put object here);
  });


});